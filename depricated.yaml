Depricated yaml

############
# NOTIFY   #
############

# notify:
  # - name: NOTIFIER_NAME
    # platform: telegram
    # chat_id: !secret chatid    

# telegram_bot:
  # - platform: polling
    # api_key: !secret api 
    # allowed_chat_ids:
      # - !secret chatid
